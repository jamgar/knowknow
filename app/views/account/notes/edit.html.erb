<div class="mx-auto max-w-4xl px-4">
  <h1 class="text-4xl font-extrabold">Edit note</h1>
  
  <%= render "form", note: @note %>

  <br>

  <div>
    <%= button_to "Delete", account_note_path(@note), method: :delete, form: { data: { turbo_confirm: "Are you sure?" } }, class: "btn btn-error" %>
  </div>
</div>

